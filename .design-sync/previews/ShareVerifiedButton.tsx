import { ShareVerifiedButton } from 'lions-of-zion';

/**
 * Copies a canonical link to the current page. It carries no props — the label
 * and the copied URL both come from the component itself.
 */
export function Default() {
  return <ShareVerifiedButton />;
}
