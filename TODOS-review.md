# סקירה בדפדפן — רשימת משימות

רשימה ייעודית לפריטים שסומנו מול האתר החי בשרת הפיתוח. **הקובץ הזה נרשם בלבד
ולא מבוצע** — כל פריט כאן ממתין להחלטה נפרדת לפני שנוגעים בקוד.

הוא נפרד מ־[`TODOS.md`](TODOS.md), שהוא תוכנית האספקה. השניים לא חופפים: שם
נרשמות משימות שהוחלט לבנות, כאן נרשם מה שנראה בעין מול הדפדפן ועדיין לא נדון.

## איך לקרוא

- כל פריט נושא את **הראוט** שבו נצפה ואת **האלמנט** שסומן.
- אם נמסר לי נימוק — הוא נרשם כלשונו ולא מפורש מחדש.
- אם זיהיתי את הקוד שאחראי לפריט, הוא מצוין כהפניה. זיהוי כזה הוא הצעה
  לנקודת התחלה, לא קביעה שזה התיקון.
- פריט שלא ברור לי מספיק נרשם עם סימן שאלה מפורש במקום ניחוש.

## סגור — כל עשרת הפריטים בוצעו 27.8

### R-01 — `/fake-resistance`: להעלות את אינדקס הקבצים לפתיחה, עם פתיח פסיכולוגי

> **✅ בוצע 27.8 דרך R-03** (`5802dbd`). הדרישה ששרדה — שקישור ייקרא כקישור —
> מומשה בשני כרטיסי הענף בעמוד הראשי.


> **🔁 הוחלף על ידי [R-03](#r-03--fake-resistance-ארכיטקטורת-העמוד-המלאה-מחליף-את-r-01-מרחיב-את-r-02).**
> אינדקס תשעת הקבצים כבר לא עולה לראש העמוד — הוא עובר לעמוד ענף ב׳, ובמקומו
> באים שני קישורי ענפים. מה ששרד מהפריט הזה: הדרישה שקישור ייקרא כקישור,
> וההבהרה שהפתיח הפסיכולוגי מנוסח מחומר קיים. נשמר לתיעוד הנימוק בלבד.

**האלמנט:** בלוק `The files` — `SectionBlock heading="The files"`,
[`app/fake-resistance/page.tsx:253`](app/fake-resistance/page.tsx:253),
כולל רשימת `ul.fileIndex` ב־`:260` (playbook, network, ושבעת תיקי המחקר).

**הבקשה כלשונה:**
> כל האזור הזה, צריך להיות בפתיחה, יותר ברור, שיהיה ברור שזה לינקים לעמודים
> עם תוכן, עם פתיח חכם שמסביר פסיכולוגית את השיטות של המשפיענים האלה.
> ובהמשך העמוד הרחבה על השיטות.

**מצב היום — סדר ששת הבלוקים בעמוד:**

| # | בלוק | שורה |
| --- | --- | --- |
| 1 | standfirst (שתי משפטי תזה) | `:146` |
| 2 | `Case files` — שלושה תיקים מעובדים | `:155` |
| 3 | `Order of correction` | `:228` |
| 4 | **`The files` — האינדקס שסומן** | `:253` |
| 5 | `The machine` — שרשרת האספקה בת ארבעת השלבים | `:289` |
| 6 | `The tells` — החתימות החוזרות | `:311` |

**פירוק הבקשה לשלושה חלקים:**

- **להעלות את `The files` לפתיחה.** רשימת קישורים קצרה שעולה מעל שלושת
  התיקים המעובדים.
- **פתיח שמסביר את המנגנון הפסיכולוגי.** מנוסח מחדש מהחומר הקיים בעמוד
  (ראו "הובהר" למטה), לא נכתב מאפס.
- **הרחבה על השיטות בהמשך העמוד.** `The machine` ו־`The tells` כבר ממלאים
  את התפקיד הזה ויושבים בתחתית — ייתכן שחלק זה של הבקשה הוא אישור למצב
  הקיים ולא שינוי.

**רקע שהוסר בהוראת הבעלים (27.8):** קודם עמדה כאן אזהרה שהסדר הנוכחי הוא
תיקון מכוון, מגובה בהערת קוד ב־`page.tsx:139` ובכלל ה־opening ceremony
ב־`DESIGN-V2`. שניהם נמחקו בהוראה מפורשת. המשמעות המעשית: **אין יותר אילוץ
מתועד שמגביל את אורך הפתיח** — ההכרעה כמה מקום הוא תופס היא עריכתית וחופשית.

**"שיהיה ברור שזה לינקים":** היום כל פריט הוא `<a>` בתוך `li.fileIndex` עם
תיאור ב־`<span>` אחריו. הקישוריות קיימת סמנטית; מה שנטען הוא שהיא לא נקראת
כך ויזואלית. זה שינוי ב־`page.module.css` ‏(`.fileIndex`), לא בהיררכיית העמוד.

~~**⚠️ מתנגש חלקית ב־R-02.**~~ **נסגר ב־R-03:** `The machine` ו־`The tells`
נשארים בעמוד הראשי.

**הובהר (27.8) — אין כאן כתיבת תוכן חדש.** "השיטות של המשפיענים האלה" =
השיטות הפסיכולוגיות, והן **כבר כתובות בעמוד**. הבקשה היא **סידור מחדש בלבד**,
לא ניסוח תוכן חדש. המקורות הקיימים:

- `The machine` ‏([`:289`](app/fake-resistance/page.tsx:289)) — שרשרת האספקה
  בת ארבעת השלבים: seeding → amplifier networks → laundering → נשיאה
  אורגנית.
- `The tells` ‏([`:311`](app/fake-resistance/page.tsx:311)) — החתימות
  החוזרות, כל אחת מקושרת לטכניקה ב־playbook.
- ה־standfirst הקיים ‏([`:146`](app/fake-resistance/page.tsx:146)) — כבר
  מנסח את הגרעין הפסיכולוגי: "Manufactured outrage is built, not felt".

מכאן שהמשימה היא עריכת סדר וניסוח מהחומר הקיים. **אין צורך במדיניות שמות** —
הפתיח מדבר על המנגנון, לא נוקב בשמות המשפיענים, ולכן
`lib/content/fake-resistance-editorial.ts` לא מעורב.

---

### R-02 — `/fake-resistance`: להוציא את תוכן התיקים לעמוד פנימי חדש

> **✅ בוצע 27.8 דרך R-03** (`5802dbd`). התיקים עברו ל־`/fake-resistance/official-narrative`
> תחת השם `Official narrative engineering` — האפשרות שמתארת מנגנון, לא זו שמכריזה
> ורדיקט גורף. `Order of correction` עבר איתם, כך ש״שלושת התיקים למעלה״ שוב מדויק.


> **↗ נבלע ב־R-03 כ״ענף א׳״.** הפריט עצמו תקף — התיקים אכן עוברים לעמוד
> פנימי — אבל הוא כבר לא עומד לבדו: R-03 מוסיף לצידו ענף שני (רשתות
> חברתיות) ומגדיר מה נשאר בעמוד הראשי. הדיון על שם הכפתור ועל הצימוד של
> `Order of correction` נשאר כאן ותקף.
> **בוטל מכאן:** השאלה הפתוחה בתחתית הפריט (האם `The machine`/`The tells`
> עוברים) — R-03 הכריע שהם נשארים.

**האלמנט:** `article#arma3-footage` — Exhibit A, אחד משלושה תיקים שנרנדרים
בלולאה מ־`edition.cases` ב־[`app/fake-resistance/page.tsx:157`](app/fake-resistance/page.tsx:157),
מקורם ב־`lib/content/fake-resistance.ts`.

**הבקשה כלשונה:**
> וכל הסוג תוכן הזה של העמוד, שהוא לא קישורים לתחקירים, צריך להיות בעמוד
> פנימי תחת כפתור בשם ״הנדסת התודעה הרשמית״ או ״השקרים של המדיה הרשמית״
> באנגלית כמובן.

**מה נכנס להגדרה "לא קישורים לתחקירים":**

| בלוק | שורה | סוג | לעבור? |
| --- | --- | --- | --- |
| standfirst | `:138` | תוכן | ? |
| `Case files` — שלושת ה־Exhibits | `:147` | תוכן | **כן** — זה מה שסומן |
| `Order of correction` | `:220` | תוכן נגזר | **כן, בעל כורחו** — ראו צימוד |
| `The files` — האינדקס | `:245` | קישורים | **לא** — נשאר, ועולה לראש (R-01) |
| `The machine` | `:281` | תוכן | ? מתנגש ב־R-01 |
| `The tells` | `:303` | תוכן + קישורים ל־playbook | ? מתנגש ב־R-01 |

**צימוד שאסור לפספס:** `Order of correction` נגזר מ־`edition.cases` — אותו
מערך בדיוק שמזין את שלושת ה־Exhibits — והטקסט שלו פותח ב־"The three files
**above**". אם התיקים עוברים והוא נשאר, הוא מצביע על תוכן שאינו בעמוד. או
שהוא עובר איתם, או שהניסוח שלו נכתב מחדש.

**שם הכפתור — נדרשת הכרעה עריכתית.** שתי האפשרויות שנמסרו, באנגלית:

- ״הנדסת התודעה הרשמית״ → `Official narrative engineering` / `Manufacturing
  the official consensus`
- ״השקרים של המדיה הרשמית״ → `The official media's lies` / `What the
  mainstream media got wrong`

**הערה שצריכה החלטה מודעת, לא בחירת נוסח:** האפשרות השנייה היא ורדיקט גורף
מראש. שאר האתר עובד בשיטה ההפוכה — כל טענה מקבלת דירוג נפרד
(`VerificationBadge` על תשעה ערכים, evidence class, confidence), ו־`CLAUDE.md`
קובע שהדירוגים לעולם לא מועלים. כותרת שמכריזה "lies" על גוף שלם קובעת
מראש את מה שהתיקים אמורים להוכיח אחד־אחד — וגם חלה על תיקים שהדירוג שלהם
אינו "false" (Exhibit B, למשל, הוא תיקון של מקור שתייג בעצמו). האפשרות
הראשונה מתארת מנגנון ולא מחלקת ורדיקטים, ולכן עקבית עם השיטה. **ההכרעה
שלך — רק שתיעשה בעיניים פתוחות.**

**עבודה נלווית שנשכחת בקלות** (ראוט חדש = לא רק קובץ עמוד):

- `app/sitemap.ts` — הראוט החדש לא ייכנס לבד.
- `scripts/ci-smoke.mjs` — מערך `ROUTES` מתוחזק ידנית; לפי `CLAUDE.md`
  ראוט חדש נבדק ב־smoke **רק אם מישהו זוכר להוסיף אותו**.
- `alternates.canonical` + JSON-LD בסוג המתאים, כמו יתר הראוטים.
- `defaultNodes` **לא** משתנה — נשאר שמונה. זה ראוט־בן, לא יעד תשיעי,
  בדיוק כמו `/fake-resistance/playbook` ו־`/network`.

~~**פתוח לשאלה:** האם ה־standfirst וה־`The machine`/`The tells` עוברים גם
הם?~~ **נסגר ב־R-03:** נשארים בעמוד הראשי, אחרי שני קישורי הענפים.

---

### R-03 — `/fake-resistance`: ארכיטקטורת העמוד המלאה (מחליף את R-01, מרחיב את R-02)

> **✅ בוצע 27.8** (`5802dbd`, `751f204`). חמשת החלקים בעמוד הראשי; שני ראוטי הענף
> `official-narrative` ו־`social-media` (״The social-media front״) עם sitemap,
> ‏`ROUTES`, canonical, JSON-LD ו־breadcrumb. `defaultNodes` נשאר שמונה.
> חלק 2 נכתב כפסיקת הבעלים, בניסוח שמאשר את טענת התשתית ומסמן במפורש את
> טענת הנרטיבים־מראש כשאלת מחקר פתוחה — ראו R-04.


**סטטוס:** זהו המפרט הקובע לעמוד. R-01 ו־R-02 נבלעים בתוכו — ראו הערות שם.

**הבקשה כלשונה:**
> אוקיי, בעצם גם כל המשפיעני תודעה יהיו בעמוד פנימי, כל מה שקודם סימנתי לך.
> זאת אומרת העמוד כאן נפתח, הסבר על Fake Resistance
> הסבר על ״מלחמת התודעה״ ואיך כל השקרים כבר היו מוכנים לפני ה-7 לאוקטובר,
> ומסביר שיש כאן 2 ענפים של תחקירים וחשיפות - הצד העיתונאי הרשמי- הצד של
> הרשתות החברתיות.(ואז את 2 הקישורים) ואחריהם.
> עוד הסבר פסיכולוגי של השיטות השפעה ומנפולציה.
>
> תציין שזה אמור להיעשות באופן חכם ומרשים במחינת ui ux שלא מבלבל או מפזר.
> לשיקול העורך באותו רגע.

**מבנה העמוד הראשי — חמישה חלקים בסדר הזה:**

| # | חלק | מקור |
| --- | --- | --- |
| 1 | הסבר על Fake Resistance — מה זה | קיים חלקית ב־standfirst |
| 2 | הסבר על "מלחמת התודעה" + שהשקרים היו מוכנים מראש לפני 7.10 | **תוכן חדש** ⚠️ |
| 3 | הסבר שיש שני ענפי תחקיר: עיתונאי רשמי / רשתות חברתיות | תוכן חדש |
| 4 | **שני הקישורים** לשני עמודי הענפים | חדש |
| 5 | הרחבה פסיכולוגית על שיטות השפעה ומניפולציה | `The machine` + `The tells` |

**שני הענפים:**

- **ענף א׳ — הצד העיתונאי הרשמי.** עמוד R-02: שלושת ה־Exhibits
  (`edition.cases`) + `Order of correction`. שם מוצע: `Official narrative
  engineering` (ראו הדיון בשם ב־R-02).
- **ענף ב׳ — הצד של הרשתות החברתיות.** תוכן המחקר על משפיעני התודעה: מה
  שיושב היום ב־`The files` — playbook, network, ושבעת תיקי המחקר. הראוטים
  `/fake-resistance/playbook`, `/network`, `/cases/<slug>` **כבר קיימים**;
  הענף הוא עמוד אינדקס מעליהם. **שם עדיין לא נקבע.**

**✅ ההתנגשות בין R-01 ל־R-02 נפתרה כאן.** "ואחריהם" קובע ש־`The machine`
ו־`The tells` **נשארים בעמוד הראשי**, אחרי שני הקישורים. אין צורך להכריע
שוב.

**חלק 2 — הוכרע על ידי הבעלים (27.8). לא חסום.**
> "מה שכתבתי זאת הפסיקה, אני המנהל והמפתח, וזה ישונה במדיניות. להסיר
> חסימות, ולבצע מה שכתבתי."

החלק נכתב כפי שנוסח. המקורות מובאים במקביל דרך **[R-04](#r-04)**, שהיא
משימת מחקר עומק עצמאית ולא תנאי מוקדם — R-03 מתקדם בלעדיה.

לתשומת המבצע, כרקע ולא כעיכוב: `CLAUDE.md` ו־`TODOS.md` מנסחים כיום
"אין עובדות מומצאות" ו"אין פרסום ללא מקורות, סטטוס ומועד עדכון". הבעלים
הודיע שהמדיניות תעודכן. **מי שמעדכן את המדיניות צריך לעשות זאת במפורש
באותם קבצים**, אחרת הם ימשיכו לסתור את העמוד ויחזירו את הדיון הזה בסבב
הבא.

**הנחיית UI/UX (כלשון הבעלים):** לבצע *באופן חכם ומרשים*, שלא מבלבל ולא
מפזר — **לשיקול העורך באותו רגע**. כלומר אין כאן מפרט עיצובי מחייב; שני
הקישורים הם רגע ההכרעה של העמוד וצריכים להיקרא ככאלה, לא כשתי שורות
ברשימה. גם הדרישה מ־R-01 — "שיהיה ברור שזה לינקים לעמודים עם תוכן" — חלה
כאן, ועכשיו על שני קישורים במקום תשעה, מה שמקל.

**עבודה נלווית:** שני ראוטים חדשים, לא אחד. כל אחד צריך `sitemap.ts`,
`ROUTES` ב־`ci-smoke.mjs` (מתוחזק ידנית), canonical ו־JSON-LD.
`defaultNodes` נשאר שמונה.

---

### R-04 — משימת מחקר עומק: האם התשתית והנרטיבים היו קיימים לפני 7.10?

> **✅ בוצע 27.8** (`3747f62`, `21dd9ed`). התיק נחת כ־
> `cases/pre-october-infrastructure.json` עם `lifecycle: "held"` — מקומט ולא
> מרונדר, ממתין להכרעת בעלים. **20 מקורות, 100% מהם עם לכידת Wayback מאומתת.**
>
> **התוצאה מפצלת את הטענה, כפי שהמשימה דרשה:**
> א׳ תשתית — `documented`, ביטחון גבוה (Doppelgänger ממאי 2022, Spamouflage
> מ־2019 עם ההסרה הגדולה ביותר חמישה שבועות לפני המתקפה, ארבעה סוגי רשומה
> בלתי־תלויים). ב׳ חומר ממוחזר — `documented` (הטכניקה הייתה שגרתית עד כדי כך
> שהאולפן ביקש להפסיק כבר בנובמבר 2022). **ג׳ נרטיבים מראש — לא בוססה:** לא
> נמצאה שום רשומה מתוארכת של טענה שנוסחה, נזרעה או תוזמנה מראש. ד׳ מהירות —
> `inferred` ושנוי במחלוקת, לא הועלה.
>
> **ארבע סתירות נמצאו ונרשמו**, החדה שבהן MTAC של מיקרוסופט (2.2024): השחקנים
> האיראניים היו תגובתיים בשלב הראשון, "באופן המעיד על תיאום מועט או אפסי עם
> חמאס".
>
> **ההשלכה על העמוד:** חצי התשתית עלה מ״נטען״ ל״מתועד״ עם התאריכים.
> על חצי הנרטיבים **הכריע הבעלים באותו יום שלא לכתוב דבר** — לא את הטענה ולא
> את הממצא השלילי — והפסקה הוסרה. הבלוק נגמר על טענת התשתית שהוא יכול לתעד.
> המחקר עצמו שמור ולא השתנה. הנימוק המלא ורשומת ההיפוך ב־`.ai/DECISIONS.md`.

**סוג:** מחקר (deep research), לא עבודת פיתוח. **אינה חוסמת דבר** — R-03
מתקדם במקביל לפי פסיקת הבעלים. זו משימת ביסוס, לא שער.
**מקור:** הוראת בעלים — "להביא מקורות. תנסח את זה כמשימה בפני עצמה. דיפ ריסרץ".

**למה זה משימה נפרדת:** R-03 הוא סידור מחדש של חומר קיים; חלק 2 שלו לבדו
הוא טענה עובדתית חדשה. אי אפשר לספק אותה בעריכה — היא מצריכה מקורות. לכן
היא נחתכת החוצה לכאן, ו־R-03 יכול להתקדם בלעדיה (עם ניסוח זמני או בלי
החלק הזה) במקום להיתקע כולו.

#### שאלת המחקר

מנוסחת כשאלה, לא כמסקנה. **המחקר חייב להיות מסוגל לחזור בתשובה שלילית** —
בריף שמניח את מסקנתו יחזיר בדיוק את סוג הטענה שהעמוד הזה נועד לתפוס.

> באיזו מידה תשתית ההפצה, הרשתות והנרטיבים ששימשו להפצת מידע כוזב אחרי
> 7.10.2023 היו קיימים, פעילים או מוכנים לפני התאריך הזה — ומה בדיוק ניתן
> להוכיח מכל אחת מהטענות הנפרדות שמרכיבות את השאלה?

#### פירוק לארבע טענות נפרדות — לכל אחת רף ראיות אחר

הטענה "כל השקרים היו מוכנים מראש" היא ארבע טענות שונות שנשמעות כאחת. הן
לא עומדות או נופלות יחד, ואסור לדווח עליהן כגוש:

| # | הטענה | רף | הערכה מוקדמת |
| --- | --- | --- | --- |
| א׳ | **תשתית** הפצה קיימת פעלה לפני 7.10 — רשתות, חוות חשבונות, מנגנוני מדינה | נמוך יחסית; ספרות אקדמית ו־OSINT ענפה | סביר שיימצא מתועד |
| ב׳ | **חומר ממוחזר** ישן שימש כחומר גלם מיד אחרי | נמוך; כבר מתועד בעמוד עצמו | Exhibit A (Arma 3, 2013) הוא בדיוק זה |
| ג׳ | **נרטיבים ספציפיים** נוסחו או הוכנו מראש לפני 7.10 | **גבוה** — זו הטענה החזקה | לא ידוע; זה לב המחקר |
| ד׳ | **מהירות** ההפעלה אחרי 7.10 מעידה על תיאום מוקדם | זו **הסקה**, לא תיעוד | חייב להיות מסומן ככזה |

**זו ההבחנה המרכזית של המשימה.** א׳ ו־ב׳ הן טענות על *מנגנון שהיה קיים*;
ג׳ היא טענה על *כוונה מוקדמת*; ד׳ היא *מסקנה מהתנהגות*. ערבוב ביניהן הוא
בדיוק הכשל שהאתר מתעד אצל אחרים.

#### סיווג ראיות — להשתמש במה שכבר קיים

`components/content/EvidenceGrade.tsx` כבר מגדיר שלוש מחלקות:
**documented / observed / inferred**. כל ממצא חוזר עם מחלקה, ו־`CLAUDE.md`
קובע שדירוגים **לעולם לא מועלים**. טענה ד׳ היא `inferred` בהגדרה ואסור
שתדווח כ־`documented` גם אם היא משכנעת.

#### תוצר נדרש

חבילת case בסכימה הקיימת — `content-packages/fake-resistance/cases/*.json`,
אותם שדות בדיוק (`question`, `scope`, `publicInterestBasis`, `confidence`,
`bottomLine`, `limitations`, `unknowns`, `contradictions`, `wouldChange`,
`sources`, `chronology`). **לא להמציא פורמט חדש** — הצינור והרנדרר קיימים,
ו־`scripts/import-research-cases.mjs` כבר יודע לקרוא את השדות האלה.

שלושה שדות בסכימה הם אפרט ההפרכה ואינם אופציונליים כאן:
- `contradictions` — ראיות שנמצאו ו**סותרות** את הטענה. אם ריק, לציין
  במפורש שחיפשו ולא נמצא, ולא להשאיר ריק בשתיקה.
- `unknowns` — מה נשאר לא ידוע.
- `wouldChange` — מה ישנה את ההערכה. אם אי אפשר לנסח כזה דבר, הטענה אינה
  טענה אמפירית ואין לפרסם אותה.

#### מקורות — סדר עדיפות

1. תיעוד ראשוני מתוארך שקדם ל־7.10 (ארכיוני פוסטים, snapshot-ים, רישומי
   דומיין, תאריכי יצירת חשבונות).
2. מחקר אקדמי ודוחות מכוני מחקר על מבצעי השפעה מתואמים.
3. דיווחי פלטפורמות (takedown reports) ו־fact-checkers מבוססים.
4. עיתונות חוקרת עם מתודולוגיה גלויה.

**כל מקור עם `archiveUrl`.** שאר העמוד כבר מקפיד על זה — `archiveUrl`
אומת ידנית מול Wayback availability API בסבב קודם, ואותה רמה נדרשת כאן.
מקור שאי אפשר לארכב לא נכנס.

#### גבולות — מה לא לעשות

- **לא לנקוב בשמות פרטיים** של אנשים שאינם דמויות ציבוריות. מדיניות השמות
  ב־`lib/content/fake-resistance-editorial.ts` חלה, כולל `isSuppressed`
  ו־`suppressionReason`.
- **לא לייבא `evidence/**` גולמי** — הצינור לוקח `publication_wording`
  ולעולם לא את שדה ה־`analysis` הפנימי.
- **לא להסיק כוונה ממהירות** בלי לסמן זאת כ־`inferred`.
- **לא להרחיב** לשאלת האחריות ל־7.10 עצמו. השאלה כאן היא על תשתית מידע
  ונרטיבים בלבד; הרחבה מעבר לכך היא נושא אחר ובעל רגישות אחרת לגמרי.

#### תלות

**אין.** R-03 מתקדם בלי להמתין לתוצאות. המחקר מבסס בדיעבד את חלק 2 ומספק
לו `SourceList` ודירוג כשיחזור.

---

### R-05 — הארכיון: להוריד את הקרדיט מ־1,177 הרשומות ולהחליפו בשיתוף

> **✅ בוצע 27.8** (`fa7f0cf`). `hamas-massacre` איבד את הפוטר ואת זוג ה־`Archive`
> בכותרת; `october7` שמר שורת קרדיט אחת מוקטנת עם הקישור והכתובת. ה־JSON-LD
> ושישה קרדיטי המדיה בגוף לא נגעו, כפי שהוכרע. במקומם `ShareRecord.tsx`:
> ‏X עם מלל מלא, פייסבוק דרך תגי OG (שכבר היו תקינים), ואינסטגרם דרך
> `navigator.share()` בנייד והעתקת קאפשן בדסקטופ — **לא כפתור שמתחזה לפרסום
> בלחיצה.** משפט העידוד: ״This record is kept public so it can be seen —
> sharing it carries it further.״
>
> **ממצא שלא נצפה בפריט:** פירורי הניווט של october7.org היו מוטמעים ב־367 מתוך
> 505 גרסאות ה־`full_text`. הרנדרר מסתיר אותם, אבל פוסט השיתוף נבנה מ־`full_text`
> — כלומר הם היו מגיעים לתוך הציוצים. `stripSourceBreadcrumb` מנקה אותם, עם טסטים.

**הראוט:** כל רשומות הארכיון —
`/october-7/documentation/[category]/[slug]` ו־`/october-7/testimonies/...`,
כולל וריאנטי `[locale]`.

**האלמנט:** `footer.provenance` —
[`components/archive/ArchiveRecord.tsx:155`](components/archive/ArchiveRecord.tsx:155).
שתי פסקאות: "Archived from {sourceLabel}. This record is reproduced as
published — its text, its media and its credits are unaltered." ואחריה
"Source record: <a>".

**הבקשה כלשונה:**
> בכל הארכיונית הפנימיים יש קרדיט מיותר, כי החומר במקור לא של מי שמצויין
> שם, אלה ציבורי. זאת אומרת כל מקום שיש קרדיט מהסוג הזה ביותר מ335 דפים
> צריך לרדת, במקום זה צריך להיכנס כפתור שיתוף חכם ועידוד שיתוף והפצה.
>
> כפתור שיתוף - אוטומטי בלחיצה לx עם המלל של התיעוד . וכפתורים גם
> לאינסטגרם ופייסבוק.

**היקף אמיתי:** הרכיב אחד, אבל הוא מוגש ל־**1,177 עמודי רשומה** (505 גרסאות
october7 + 670 hamas-massacre + 2 אינדקסים). שינוי בקובץ אחד, אפקט על כל
הארכיון. אין צורך לגעת ב־335 קבצים.

#### ✅ תוקן בהוראת הבעלים (27.8) — הפריט מתפצל לפי חבילה

> "כל מה ששייך ל־www.october7.org בלבד. אפשר להשאיר קרדיט קטן עם קישור
> לחיץ וכתובת האתר."

**זו הבהרה שמצמצמת את הפריט לחצי, ומשנה את היקפו מהותית:**

| חבילה | ראוט | רשומות / גרסאות | ההוראה |
| --- | --- | --- | --- |
| **october7** ‏(`October7.org`) | `/october-7/testimonies` | 179 / **505** | ✅ **הקרדיט נשאר** — קטן, עם קישור לחיץ וכתובת האתר |
| **hamas-massacre** ‏(`Hamas-Massacre.net`) | `/october-7/documentation` | 335 / **670** | ❌ הקרדיט יורד |

**הפיצול קל לביצוע — התווית כבר פר־ראוט.** הקבוע `SOURCE_LABEL` מוגדר
בארבעה קובצי ראוט, שניים לכל חבילה:

- `app/october-7/testimonies/[slug]/page.tsx:5` → `'October7.org'`
- `app/october-7/testimonies/[slug]/[locale]/page.tsx:5` → `'October7.org'`
- `app/october-7/documentation/[category]/[slug]/page.tsx:5` → `'Hamas-Massacre.net'`
- `app/october-7/documentation/[category]/[slug]/[locale]/page.tsx:8` → `'Hamas-Massacre.net'`

אין צורך בזיהוי חבילה חדש — הוא כבר קיים בנקודת הקריאה.

**✅ הוכרע: קרדיט אחד בלבד.** `sourceLabel` מרונדר היום ב**שני** מקומות —
בטבלת המטא של הכותרת ([`ArchiveRecord.tsx:80`](components/archive/ArchiveRecord.tsx:80),
כזוג `<dt>Archive</dt><dd>`) ובפוטר ([`:155`](components/archive/ArchiveRecord.tsx:155)).
הבעלים הורה **"לשמור אחד"**.

הנותר חייב להיות זה שנושא **קישור לחיץ + כתובת האתר**, כי זה מה שההוראה
מתארת — והיום רק הפוטר נושא את שניהם; זוג המטא הוא טקסט בלבד. כלומר:

- **october7** — פוטר מוקטן עם קישור **נשאר**; זוג ה־`Archive` בכותרת **יורד**.
- **hamas-massacre** — שניהם יורדים.

אם הכוונה הייתה הפוכה (לשמור את זוג המטא הקצר ולוותר על הקישור) — זו מילה
אחת לתיקון, אבל אז אין קישור לחיץ ברשומה כלל.

**✅ נכתב.** רשומת ההיפוך נוספה ל־[`.ai/DECISIONS.md`](.ai/DECISIONS.md)
בתאריך 27.8 ("Archive attribution splits by package"), כולל הנקודה שהקרדיט
של october7 הופך לקישור לחיץ — מהלך הפוך לכלל "nothing in a record body is a
hyperlink" — ולמה זו נגזרת ולא סתירה. גם היא מסומנת כ**טרם מיושמת**.

#### מה יורד

- `footer.provenance` על שתי פסקאותיו, כולל הקישור ל־`source_url` —
  **ב־hamas-massacre בלבד** (335 רשומות / 670 גרסאות). ב־october7 הוא
  נשאר בגרסה מוקטנת עם קישור.
- הסגנונות `.provenance`, `.provenance p`, `.provenance a` —
  [`archive.module.css:355-380`](components/archive/archive.module.css:355).
- הערות התיעוד שמתארות את ההתנהגות הזו:
  `ArchiveRecord.tsx:118-122` ו־`ArchiveBlocks.tsx:36,71`.

**מהפך מוצהר:** זו היפוך של החלטה מתועדת. `CLAUDE.md` קובע היום "nothing in
a record body is a hyperlink **while credits always render** — provenance
travels in the footer and in JSON-LD", ו־`.ai/DECISIONS.md` (26.8.2026)
מציין שניסוח־מחדש כדי להתחמק מייחוס **נשקל ונדחה**. הבעלים הכריע אחרת.
**מי שמבצע צריך לעדכן את `CLAUDE.md` ולצרף רשומת היפוך ל־`.ai/DECISIONS.md`**
— היומן הוא append-only, כלומר מצרפים היפוך ולא מוחקים את המקורי.

**לבדיקה לפני ביצוע — לא חסימה, פריט עבודה:**
`ArchiveRecordPage.tsx:199-208` פולט JSON-LD מסוג `ArchiveComponent` עם
`isBasedOn` ו־`holdingArchive`, ו־`:203` נושא `datePublished`. הייחוס חי גם
שם, לא רק בפוטר. להחליט אם ה־JSON-LD יורד יחד עם הפוטר או נשאר — הוא לא
נראה לקורא, אבל הוא כן מה שמצהיר למנועי חיפוש שהרשומה משוכפלת ולא מקורית.

**נפרד וטרם הוכרע:** קרדיטי מדיה בתוך גוף הרשומה
([`ArchiveBlocks.tsx:249`](components/archive/ArchiveBlocks.tsx:249)) הם
שדה אחר (`block.credit`) ולא הפוטר שסומן. הם נוגעים ל־**3 מתוך 528** פריטי
מדיה ב־hamas ו־**3 מתוך 499** ב־october7 — כלומר שישה במצטבר. לא נגעתי בהם.
לומר אם גם הם יורדים.

#### מה נכנס במקום

עידוד שיתוף והפצה + כפתורי שיתוף. רכיב חדש הוצע בעבר לארכיון, אך עדיין לא
נוצר (`ShareRecord`, רכיב לקוח הנדרש ל־clipboard
ול־`navigator.share`). ה־URL הקנוני כבר מחושב ב־`ArchiveRecordPage.tsx:164`,
וטקסט הקאפשן זמין ב־`version.title` / `version.excerpt` / `version.full_text`.

**מצב הפלטפורמות — עובדתי, קובע מה ניתן לבנות:**

| יעד | פרסום אוטומטי עם המלל? | המנגנון האמיתי |
| --- | --- | --- |
| **X** | ✅ **כן, בדיוק כמבוקש** | `x.com/intent/post?text=…&url=…` ממלא את המלל מראש |
| **פייסבוק** | ⚠️ **לא** | `sharer.php?u=…` מעביר קישור בלבד. פייסבוק חסמה מילוי מלל מראש; הטקסט נלקח מתגי ה־OG של העמוד |
| **אינסטגרם** | ❌ **לא קיים** | לאינסטגרם **אין בכלל web intent**. אין URL שפותח אותה עם פוסט מוכן |

**השלכה על פייסבוק:** כדי שהשיתוף שם יישא מלל אמיתי, תגי ה־`openGraph` של
עמוד הרשומה הם מה שקובע — לא הכפתור. שווה לוודא שהם נושאים את כותרת
הרשומה ואת התקציר.

**שתי הדרכים האמיתיות לאינסטגרם** (לבחירת הבעלים — טרם נבחר):
1. **נייד:** כפתור שקורא ל־`navigator.share()` ופותח את תפריט השיתוף של
   המערכת, שאינסטגרם מופיעה בו אם מותקנת. זו הדרך הקרובה ביותר לבקשה.
2. **דסקטופ:** העתקת הקאפשן ללוח + פתיחת אינסטגרם, כשההדבקה ידנית.

**מה שלא ייבנה בלי הוראה מפורשת:** כפתור אינסטגרם שנראה כמו "פרסום בלחיצה"
ואינו כזה. יש כבר תקדים לכשל הזה בקוד — `XPublicAuthControl` מציג
"Continue with X" תקין למראה גם כשה־credentials חסרים — והאתר מצהיר על
עיקרון "No false live state".

#### פתוח להכרעה

1. איזו משתי הדרכים לאינסטגרם.
2. האם ה־JSON-LD ‏(`isBasedOn` / `holdingArchive`) יורד עם הפוטר.
3. האם שישה קרדיטי המדיה בגוף הרשומות יורדים גם הם.
4. נוסח משפט העידוד לשיתוף.

---

### R-06 — כפתור החזרה קופץ הביתה במקום שלב אחד אחורה

> **✅ בוצע 27.8** (`7f10242`, `751f204`). היעד נגזר מהפריט האחרון ב־breadcrumb,
> עם נפילה חזרה ל־`/` בהיעדרו, והתווית משתנה איתו. `SectionPage` קיבל את ה־prop
> שחסר לו, כך ש־`/fake-resistance/**` כבר לא האזור היחיד בלי breadcrumb.
> שמונת עמודי היעד, `/methodology` ו־`/corrections` לא נגעו.


**האלמנט:** `a.identityExit` — "← Back to the scan", בתוך רצועת הזהות.
מקודד קשיח ל־`href="/"` בשני מקומות:
[`DocPage.tsx:174`](components/sections/DocPage.tsx:174) ו־
[`SectionPage.tsx:143`](components/sections/SectionPage.tsx:143).

**הבקשה כלשונה:**
> כאן צריך להיות כפתור חזרה שלב אחד אחורה ולא ישר לעמוד הבית.

**נצפה ב־** `/october-7/testimonies/<slug>` — רשומה בעומק שלוש רמות, שממנה
"חזרה" מדלגת מעל אינדקס העדויות ומעל `/october-7` ישר לבית.

**התיקון קטן מכפי שהוא נשמע — הנתון כבר קיים.** `ArchiveRecordPage.tsx:115`
כבר מעביר ל־`DocPage` את `breadcrumb={archiveTrail(pkg)}`, ו־`DocPage:161`
כבר מרנדר אותו כ־`<nav aria-label="Breadcrumb">`. כלומר **ההורה כבר ידוע
לרכיב** — הוא פשוט לא משמש את כפתור החזרה. היעד הנכון הוא הפריט האחרון
ב־`breadcrumb`, ורק בהיעדרו נופלים חזרה ל־`/`.

**היקף — לא לשנות גלובלית:** `identityExit` משותף לכל העמודים.

| סוג עמוד | ההורה האמיתי | היום |
| --- | --- | --- |
| שמונת עמודי היעד | הבית | ✅ נכון |
| `/methodology`, `/corrections` | הבית | ✅ נכון |
| אינדקסי הארכיון | `/october-7` | ❌ קופץ הביתה |
| רשומות ארכיון (1,175) | האינדקס שלהן | ❌ קופץ הביתה |
| `/fake-resistance/playbook`, `/network`, `/cases/*` | `/fake-resistance` | ❌ קופץ הביתה |

**שים לב:** שלושת עמודי המחקר תחת `/fake-resistance` משתמשים ב־`SectionPage`
שאין לו בכלל prop של breadcrumb — כלומר שם צריך גם להוסיף את המנגנון, לא רק
לחווט אותו. (זה גם הפער שנמצא קודם: `/fake-resistance/**` הוא היחיד ללא
breadcrumb.)

**ניסוח:** אם הכפתור מפסיק להוביל הביתה, "Back to the scan" הופך למטעה.
הטקסט צריך להשתנות לפי היעד.

---

### R-07 — מדיה שבורה ברשומות הארכיון

> **✅ בוצע 27.8** (`26c4845`, `7b744f2`). האבחנה אושרה: זו הייתה תקלת סביבה —
> `NEXT_PUBLIC_ARCHIVE_CDN` נוסף ל־`.env.local`. שתי הבעיות האמיתיות טופלו:
> `verify-archive-assets.mjs` מחווט ל־CI כ־job מקבילי (מדגם 80, ‏~10 שניות),
> והדרישה המקומית מתועדת ב־`environment.md` ו־`operations.md`.
> **הריצה המלאה שנשארה פתוחה בפריט הזה בוצעה: 2,018 נבדקו / 0 לא נגישים.**


**האלמנט:** `p.externalMedia` — "An image published with this record is not
loading from this archive." נוצר ב־
[`ArchiveImage.tsx:56`](components/archive/ArchiveImage.tsx:56).

**הבקשה כלשונה:**
> בנוסף צריך לבדוק בכל דף שיש בעיה כזאת לטפל בה. שקישור שבור.

#### ⚠️ מה שראית הוא כמעט בוודאות תקלת סביבה מקומית, לא תקלה באתר

בדקתי לפני שניסחתי, כי זה משנה את המשימה מהיסוד:

- **`NEXT_PUBLIC_ARCHIVE_CDN` לא מוגדר ב־`.env.local`** של ה־worktree הזה.
- **`public/archive/` לא קיים** — וזה הנתיב שאליו נופלים כשהמשתנה חסר
  (ה־`src` בצילום היה `/archive/october7/originals/...`, כלומר בדיוק
  ה־fallback).
- `ArchiveImage` מחזיק `useState(failed)` ומחליף לטקסט **ב־`onError` בצד
  הלקוח** ‏(`:53-57`).

**מכאן: מקומית, כל התמונות בכל 1,177 הרשומות מציגות את ההודעה הזו.** זה לא
מדגם של דפים שבורים — זה 100%, וזה ייעלם ברגע שהמשתנה יוגדר. בפרודקשן הוא
מוגדר על Preview ו־Production (`.ai/STATE.md`).

**לתיקון מיידי אצלך:** להוסיף את המשתנה ל־`.env.local` ולהפעיל מחדש. שים לב
ש־`NEXT_PUBLIC_` מוחלף **בזמן build**, כך ששינוי שלו מחייב הפעלה מחדש של
השרת ולא רק רענון.

#### שתי בעיות אמיתיות שכן עלו מהבדיקה

1. **הסביבה המקומית לא מתועדת ככזו שדורשת את המשתנה.** מפתח שמושך את
   הריפו ומריץ `npm run dev` רואה ארכיון שבור לגמרי בלי שום הודעת שגיאה
   שמסבירה למה. `.env.example` אינו tracked (‏`.gitignore` תופס `.env*`),
   אז אין מאיפה ללמוד את זה.

2. **כשל ה־CDN שקט לחלוטין בפרודקשן — וזו הבעיה החמורה.** אם המשתנה יוגדר
   שגוי, העמודים ייבנו, הבדיקות יעברו, הטקסט יוצג, ורק המדיה תיעלם — בדיוק
   ההתנהגות שראית. הכלי שנבנה לתפוס את זה, `scripts/verify-archive-assets.mjs`,
   הוא היחיד שלא דורש דפדפן ולכן רץ על Linux — **ולפי `CLAUDE.md` הוא
   "CI-unwired"**, כלומר קיים ולא מחובר. לחווט אותו ל־CI זה התיקון האמיתי
   של "לבדוק בכל דף שיש בעיה כזאת".

#### תקלה מתועדת ונפרדת — לא באג

שני סרטונים בחבילות **אין להם `package_path`**: המקור מארח אותם ביוטיוב
והחבילה רושמת אותם בלי להוריד. הם מציגים הודעה דומה במכוון, אחרי שה־build
הראשון קרס עליהם. אלה שניים ידועים, לא חלק מהבעיה.

#### מה שצריך להיבדק ועדיין לא נבדק

הרצת `node scripts/verify-archive-assets.mjs <cdn-base>` מול ה־CDN החי תיתן
את התשובה האמיתית לשאלה "בכל דף שיש בעיה". בסבב קודם היא החזירה 2,018
נבדקו / 0 לא נגישים, אבל זה היה **לפני** הסבבים שאחריו. לא הרצתי אותה כאן
כי אין CDN base בסביבה הזו.

---

### R-08 — פריסת שורות האינדקס: תצוגה עשירה יותר, אולי עם הצצה לתיעוד

> **✅ בוצע 27.8** (`fa7f0cf`). שורה עשירה, לא כרטיס ריחוף — כפי שהוכרע, כי
> ריחוף אין לו מקבילה במגע. תמונות הכיסוי הקיימות (514/514) מוצגות ב־w480
> מוקטן ב־CSS עם `loading="lazy"` ו־`alt=""`. `excerpt` נוסף ל־`ArchiveIndexEntry`,
> לאימפורטר, ולשני קובצי ה־index: october7 ‏59KB→90KB, ‏hamas-massacre ‏99KB→123KB,
> הסדר וכל השדות הקיימים זהים בית־בית. `showMeta` נשאר כבוי בתיעוד.
> ‏36 excerpt-ים שהיו מזוהמים בפירורי הניווט נבנו מחדש; רשומה אחת בלי `full_text`
> נשארה בלי excerpt במקום לחתוך באמצע משפט של ניצול.

**האלמנט:** `a.recordLink` —
[`ArchiveRecordList.tsx:60-73`](components/archive/ArchiveRecordList.tsx:60).
נצפה ב־`/october-7/documentation`, חל גם על `/october-7/testimonies`.

**הבקשה כלשונה:**
> אופן הפריסה של כל קישור צריך להיות אחר ולהיראות יותר טוב, אם יש אפשרות
> עם חלון ריחוף שמראה הצצה לעמוד הפנימי עם התיעוד, שרואים גם את התיעוד.
> או בחלון של הקישור הזה עצמו.

**מה השורה מציגה היום:** מספר (`001`) + כותרת + חץ. באינדקס העדויות מתווספת
שורת מטא (עד ומספר שנה).

#### ✅ הממצא המרכזי: תמונת כיסוי כבר קיימת לכל רשומה ואינה מוצגת

| חבילה | רשומות | עם `cover` | עם `witness` |
| --- | --- | --- | --- |
| october7 (testimonies) | 179 | **179 (100%)** | 179 |
| hamas-massacre (documentation) | 335 | **335 (100%)** | 0 |

`ArchiveIndexEntry` ‏([`archive.ts:90`](lib/content/archive.ts:90)) כבר נושא
`cover` כ־`media_id`. **514 מתוך 514 הרשומות נושאות אחת, ואף שורה לא מציגה
אותה.** כלומר העושר הוויזואלי שביקשת זמין **בלי שום שינוי בצינור הנתונים** —
רק חיווט של שדה קיים.

#### ⚠️ מה שלא זמין: טקסט התיעוד

השדות `excerpt` ו־`full_text` יושבים על `ArchiveVersion` שבתוך
`records/<id>.json`, ונקראים **קובץ אחד לכל עמוד**. הם אינם באינדקס. כדי
להראות "הצצה לתיעוד" עם טקסט צריך אחד משניים:

1. **להוסיף `excerpt` ל־`index.json` בזמן הייבוא** —
   `scripts/import-archive-package.mjs`. עלות חד־פעמית, מייקר את האינדקס
   הקיים (14MB), ואז ההצצה זולה לנצח. **מומלץ.**
2. לקרוא 514 קובצי רשומה בזמן build של האינדקס. יקר ומיותר.

#### עלות התמונות — הנקודה שתקבע את העיצוב

הגודל הקטן ביותר שנאפה הוא **w480** (הרוחבים הקיימים: 480 / 960 / 1600).
תמונונת בשורה רוצה ~120–160px.

- **בלי שינוי צינור:** להשתמש ב־w480 ולהקטין ב־CSS, עם `loading="lazy"`.
  עובד היום; מוריד יותר בייטים מהנדרש אבל רק לשורות שנכנסות למסך.
- **עם שינוי צינור:** לאפות נגזרת ~w160. מצריך את ה־pkgbuild שיושב **מחוץ
  לריפו** (`~/Documents/october-7_toad/pkgbuild/`) והעלאה מחדש ל־Blob.
  זו עבודה נפרדת ולא "עוד CSS".

**‏335 שורות במסך אחד** — כל פתרון חייב `loading="lazy"`, אחרת האינדקס
נעשה כבד מכל עמוד אחר באתר.

#### נגישות

`alt_text` קיים ב־**272 מתוך 499** פריטי המדיה ב־october7. עבור השאר צריך
הכרעה — ולתמונונת לצד כותרת שכבר מתארת את הרשומה, `alt=""` (דקורטיבי) הוא
ככל הנראה התשובה הנכונה ולא המצאת תיאור.

#### הצצת ריחוף — שתי מגבלות

1. **אין hover במכשירי מגע.** כל מה שמסתמך על ריחוף חייב תשובה מקבילה
   בנייד. "או בחלון של הקישור הזה עצמו" (הניסוח השני בבקשה) פותר את זה
   מיניה וביה — שורה עשירה עובדת זהה בכל מכשיר. **זו הסיבה להעדיף אותה.**
2. **לא נמצא רכיב hover-card לשימוש חוזר** ב־`components/`. כרטיסי הריחוף
   של הניווט חיים בסצנת החלקיקים ואינם רכיב DOM שניתן לשאול. כלומר זה
   רכיב חדש, לא הרכבה של קיים.

#### ⛔ מה לא לעשות

**לא להדליק `showMeta` באינדקס התיעוד.** הוא כבוי בכוונה ומתועד ב־
[`documentation/page.tsx:41-44`](app/october-7/documentation/page.tsx:41):
לרשומות האלה אין עד, כולן דו־לשוניות, והתאריך הוא חותמת הזחילה ולא תאריך
האירוע — כך שהשורה הייתה נפתרת לאותן שתי מחרוזות ב־335 השורות. **הריק הזה
מדויק, לא חסר.** מכאן שבאינדקס הזה התמונונת היא ההעשרה היחידה שיש בה תוכן
אמיתי.

#### פתוח להכרעה

1. שורה עשירה או כרטיס ריחוף (או שורה עשירה + הרחבה בריחוף בדסקטופ בלבד).
2. האם מוסיפים `excerpt` לאינדקס בייבוא — בלעדיו אין "הצצה לתיעוד" עם טקסט.
3. w480 מוקטן ב־CSS, או אפייה של נגזרת קטנה.

---

### R-09 — מקורות בתחקירים: כתובות API שלא ניתן לפתוח, ותווית מארח מיותרת

> **✅ בוצע 27.8** (`9c3f352`). 53 כתובות מתות נותקו, 48 מחרוזות נוסחו מחדש,
> 7 מזהי מקור שונו. הייחוס נשמר כטקסט עם `retrievedAt` שמוצג רק כשאין קישור.
> ‏6 קישורי הציוץ הישירים אומתו; טענות מדגם נשארו בלי קישור, במכוון.
> האימפורטר נכשל קשה אם השם חוזר, כך שהתיקון שורד ייבוא.
> **הכתובת הדנית הוחלפה בחליפה אמיתית** — הרישום עבר ל־`autregweb.stps.dk`,
> הרשומה אותרה ואומתה 200, והלכידה של הכתובת הישנה צורפה כ־`archiveUrl`.
> **Wayback: 8 מראות נבדקו, 0 עם לכידה שמישה** — היחידה שנמצאה הייתה ריקה (204)
> ולכן לא נרשמה, כדי לא להבטיח לקורא ארכיון ולתת לו עמוד ריק.


**האלמנט:** `span.sourceKind` ו־`a` בתוך `ol.sourceList` —
`components/content/SourceList.tsx`, כפי שנצפה ב־
`/fake-resistance/cases/[slug]`. הדוגמה שסומנה:
`kind = "twitterapi.io"`, ‏`url = https://api.twitterapi.io/twitter/user/info`.

**הבקשה כלשונה:**
> בכל ״ראייה״ כזאת בתחקירים, חייב להעיף את הכתובת -twitterapi.io. מיותר.
> והקישור אחרי שלוחצים עליו מביא שגיאה. אז לבדוק מה עושים אם זה, אם אין
> דרך להציג את הציוג אז להוריד גם את הקישור. יש המון צריך להשים לב טוב.

#### כמה זה "המון" — נמדד

| מדד | כמות |
| --- | --- |
| רשומות מקור ייחודיות בחבילת המחקר | **96** |
| מהן עם `kind` שהוא שם מארח חשוף | **12** — כולן `twitterapi.io` |
| הופעות של כתובת `api.twitterapi.io` בעמודים | **53 מתוך 210** (25%) |

#### שתי בעיות נפרדות, לא אחת

1. **התווית.** מתוך 96 הרשומות, רק 12 נושאות שם מארח חשוף כ־`kind`. השאר
   נושאות תיאור אמיתי — "The Jerusalem Post", "Sotwe (third-party X mirror)",
   "Danish physician authorization - Anastasia Maria Loupis". **התווית עצמה
   אינה מיותרת; היא מיותרת רק כשהיא שם מארח.** לכן זה תיקון נקודתי ולא
   הסרה של `sourceKind`.
2. **הקישור.** `api.twitterapi.io` הוא endpoint של API בתשלום הדורש מפתח.
   הוא יחזיר שגיאה **לכל קורא, בכל דפדפן, תמיד**. זה לא קישור שנשבר — זה
   קישור שמעולם לא היה ניתן ללחיצה.

#### ⛔ אזהרה קריטית למי שמבצע

הרצתי בדיקת סטטוס על 57 מארחים ייחודיים. **רבים מה־403 הם חסימות בוט של
מקורות מצוינים**, לא קישורים שבורים: NYT, Bloomberg, Reuters (401),
Times of Israel, Mediaite, Arab News, Washington Post — כולם נפתחים כרגיל
בדפדפן אמיתי.

**"להסיר כל מה שמחזיר 403" ימחק בדיוק את המקורות הטובים ביותר בתיק.**
כל בדיקה אוטומטית כאן חייבת להבחין בין חסימת בוט לבין כתובת מתה.

#### שלוש קטגוריות אמיתיות

| קטגוריה | היקף | מה לעשות |
| --- | --- | --- |
| **endpoint של API** — לעולם לא ניתן לצפייה | 53 הופעות / 12 רשומות | לנתק את הקישור, לשמור את הייחוס כטקסט |
| **מראות scraper של X** — שבירות מטבען: `sotwe.com` (40), `twstalker` (8), `nitter.app`, `twtdata`, `pscp.tv` | ~50+ | להחליט: לארכב, להחליף במקור יציב, או לנתק |
| **כתובת אחת פגומה** | 1 | `autregweb.sst.dk/Authorization.aspx?id=0BYZW` — רישום רופאים דני, `reliability: high`, `sourceRole: primary`, ומחזיר **000**. זו כתובת קשורת־סשן שלא תיפתח לאיש |

#### הפתרון המוצע — יש לו תקדים בקוד

**לנתק את הקישור ולשמור את הייחוס כטקסט רגיל.** בדיוק מה שהארכיון כבר עושה:
`CLAUDE.md` קובע ש"nothing in a record body is a hyperlink while credits
always render" — ייחוס נשמר, קישוריות לא. אותה צורת פתרון בדיוק.

הנתונים כבר תומכים בזה: לכל רשומת מקור יש `retrievedAt`, כך שאפשר להציג
"‏twitterapi.io — שאילתת API, נדלתה 26.8.2026" בלי קישור. זה **כן** מוסר את
השגיאה בלחיצה, וכן שומר על שקיפות המתודולוגיה — המחקר באמת השתמש בנתונים
האלה, והעלמתם לגמרי תסתיר את מקור הראיות.

#### ⚠️ איפה מתקנים — קובע אם התיקון שורד

**לא לערוך את קובצי ה־JSON.** `CLAUDE.md` קובע במפורש שהשיפוט העריכתי חי
ב־`lib/content/fake-resistance-editorial.ts` ומוחל **בתפר ולא בייבוא** —
"Putting it there is what stops a re-import reverting it". עריכה ישירה
ב־`content-packages/` תימחק בייבוא הבא.

התיקון הנכון: כלל בשכבה העריכתית שמסווג מקור כ"לא־לחיץ", + התאמה
ב־`SourceList` שתכבד אותו.

#### ✅ הוכרע על ידי הבעלים (27.8)

> "לגבי ב־content-packages/ תתקן ותעדכן מה שצריך שהתיקון ישרוד. ותוודא
> שאין שום אזכור לtwitterapi.io. ולבדוק אם אפשר להוסיף פשוט קישורים
> לציוץ עצמו"

**מתקנים בנתונים עצמם**, לא רק בשכבה העריכתית — ומעדכנים את מה שנדרש כדי
שהתיקון ישרוד ייבוא חוזר. כלומר שני מקומות, לא אחד:

1. `content-packages/fake-resistance/**` — הנתונים.
2. `scripts/import-research-cases.mjs` — אחרת הייבוא הבא יחזיר את מה
   שנמחק. **זה החלק שקל לשכוח והוא כל ההבדל בין תיקון לתיקון שנעלם.**

**דרישת קבלה: אפס אזכורים ל־`twitterapi`.** מיפוי נוכחי — **116 אזכורים
בעשרה קבצים**:

| קובץ | |
| --- | --- |
| `cases/hinkle-machine.json` | |
| `cases/state-media-irgc-press.json` | |
| `cases/aggregators-feeders.json` | |
| `cases/muslim-palestinian-influencers.json` | |
| `cases/manosphere-far-right.json` | |
| `cases/giant-amplifiers.json` | |
| `cases/grayzone-anti-empire.json` | |
| `network.json` | |
| `docs/fake-resistance-integration.md` | תיעוד — מזכיר את הכלי כמקור נתונים |
| `TODOS-review.md` | הקובץ הזה — תיאור המשימה, לא ממש אזכור |

בדיקת סיום: `grep -ri twitterapi --exclude-dir=node_modules .` צריך לחזור
ריק (למעט הקובץ הזה ורשומת ההיפוך ביומן).

#### 🔍 "האם אפשר פשוט לקשר לציוץ עצמו" — נבדק, והתשובה מפוצלת

| מצב | היקף | אפשר לקשר? |
| --- | --- | --- |
| קישור `x.com/<handle>/status/<id>` כבר קיים בנתונים | **6 הופעות**, בשני תיקים | ✅ **כן — כבר שם, רק לחווט** |
| רק `handle` קיים, בלי מזהה פוסט | **91 שדות `handle`** | ⚠️ אפשר לקשר לפרופיל, **אבל זו לא ראיה לטענה** |
| הטענה היא **תצפית מצרפית על מדגם** | רוב ה־exhibits | ❌ **קישור לציוץ בודד יהיה שגוי** |

**זה הממצא המהותי.** מבנה ה־exhibit הוא
`{id, statement, verdict, confidence, observedAt, attributedTo, techniques, sources}`,
ורוב ה־`statement` הם טענות סטטיסטיות — לדוגמה מתוך `hinkle-machine`:

> "In our 34-post sample from August 25-26, most Hinkle posts were
> quote-posts or reposts of WelcomeTheGulag clips published within the
> preceding hour."

**היעדר קישור לציוץ כאן אינו רשלנות — הוא נכון.** הטענה היא על *מדגם*, לא
על פוסט. קישור לציוץ אחד היה מציג ממצא מצרפי כאילו הוא ציטוט של פוסט בודד,
וזה בדיוק סוג ההטעיה שהתיקים האלה מתעדים אצל אחרים.

**מכאן שהתיקון מתפצל לשלושה:**
- **6 המקרים עם `status/`** — לחווט לקישור ישיר לציוץ. ✅
- **טענות מדגם** — להסיר את קישור ה־API ולהציג במקומו את **הגדרת המדגם**,
  שכבר כתובה ב־`statement` עצמו ("34-post sample from August 25-26"),
  יחד עם `retrievedAt`. זו הפרובננס הכנה לטענה מצרפית.
- **מראות scraper** (‏sotwe 40, twstalker 8, nitter, twtdata, pscp.tv) —
  עדיין פתוח: לארכב ב־Wayback, להחליף, או לנתק. הן רבע מהראיות בחלק
  מהתיקים, ולכן הכרעה נפרדת.

**עדיין פתוח:** הכתובת הדנית (`autregweb.sst.dk`, מחזירה 000) — למצוא
חלופה יציבה או לנתק ולהשאיר את הממצא.

---

### R-10 — הורדה ושיתוף לכל פריט מדיה, ושיתוף העדות המלאה

> **✅ בוצע 27.8** (`fa7f0cf`). **ההורדה נפתרה בדרך הזולה ביותר:** ‏Vercel Blob
> מכבד `?download=1` ומחזיר `Content-Disposition: attachment` — אומת ב־curl.
> כלומר קישור CDN רגיל, **בלי ראוט proxy**, וכלל ה־`CLAUDE.md` שמדיה לעולם לא
> עוברת דרך אפליקציית Next **נשאר בתוקף**. ההיתר ביומן נרשם כלא־ממומש.
> **הסתייגות:** הדפדפן משתמש בשם הקובץ של ה־Blob (גיבוב תוכן) בין מקורות
> ומתעלם משם מבוקש — כלומר ההפחתה של ״שם הרשומה בשם הקובץ״ מסופקת רק חלקית.
>
> גרסת הפרסום ל־X ב־`lib/content/share-text.ts` עם טסטים: חיתוך בגבול משפט
> בתוך תקציב של ~230 תווים אחרי 23 התווים של `t.co` והייחוס.

**האלמנט:** `video.video` —
[`ArchiveBlocks.tsx`](components/archive/ArchiveBlocks.tsx), נצפה ב־
`/october-7/testimonies/[slug]`. חל על תמונות וסרטונים כאחד.

**הבקשה כלשונה:**
> בכל סוג מדיה קיימת בנוסף לכפתור השיתוף המהיר עם הטקסט צריך להיות אפשרות
> הורדה למחשב. ואפשרות שיתוף של כל העדות הארוכה גם בעדויות.

**מרחיב את [R-05](#r-05).** שם השיתוף נמצא ברמת הרשומה; כאן הוא יורד לרמת
פריט המדיה הבודד, ונוסף לו כפתור הורדה.

#### שלושה מנגנונים נפרדים

| # | מה | היקף |
| --- | --- | --- |
| 1 | **הורדה** לכל פריט מדיה | ~1,027 פריטי מדיה (499 october7 + 528 hamas) |
| 2 | **שיתוף** לכל פריט מדיה | אותם פריטים |
| 3 | **שיתוף העדות המלאה** | 179 עדויות + 335 תיעודים |

#### הורדה — פשוט, ויש רק מלכודת אחת

הנכסים כבר מוגשים מ־Vercel Blob דרך `NEXT_PUBLIC_ARCHIVE_CDN`, כלומר
`<a href={cdnUrl} download>` הוא כל מה שנדרש עקרונית.

**המלכודת:** התכונה `download` **מתעלמים ממנה בין מקורות (cross-origin)**.
ה־Blob הוא דומיין אחר מהאתר, כך שדפדפן יפתח את הקובץ בלשונית במקום להוריד
אותו — אלא אם התגובה נושאת `Content-Disposition: attachment`. שלוש דרכים:

1. להגיש כותרת `Content-Disposition` מה־Blob (הכי נקי, אם ניתן להגדרה).
2. ראוט proxy קטן באפליקציה שמוסיף את הכותרת — **מתנגש** בכלל הקיים
   ש`CLAUDE.md` קובע: מדיה מוגשת מה־CDN ישירות ו**לעולם לא דרך אפליקציית
   Next**. אימוץ הדרך הזו הוא היפוך של אותו כלל, עם עלות תעבורה אמיתית על
   1.8GB.
3. הורדה בצד הלקוח דרך `fetch` → `blob` → קישור זמני. דורש CORS מתאים
   ומחזיק את הקובץ בזיכרון — בעייתי לווידאו.

**✅ הוכרע (27.8):** *"לעשות מה שצריך כדי שההורדה תתאפשר."* ההורדה חייבת
לעבוד — הדרך פתוחה לבחירת המבצע, לפי הסדר הזה:

1. **קודם לבדוק `Content-Disposition` על ה־Blob.** אם ניתן להגדרה, זו
   התשובה: אפס תעבורה דרך האפליקציה, אפס התנגשות בכללים קיימים.
2. **אם לא — ראוט proxy מותר.** הבעלים הכריע שההורדה מתאפשרת, ולכן הכלל
   ב־`CLAUDE.md` ("media is served from CDN URLs directly, never proxied
   through the Next app") **מתהפך עבור נתיב ההורדה בלבד.** שים לב שזה
   נתיב חדש בתשלום תעבורה: 1.8GB של מדיה, וכל הורדה עוברת דרך פונקציה.
   **לתחום את ההיפוך להורדה בלבד** — הצגת מדיה בעמוד נשארת ישירות מה־CDN.
3. הורדה בצד לקוח דרך `fetch`→`blob` — רק כמוצא אחרון, ולא לווידאו.

**✅ נכתב.** רשומת ההיפוך נוספה ל־[`.ai/DECISIONS.md`](.ai/DECISIONS.md)
בתאריך 27.8 ("Media may be proxied through the app, for downloads only"),
ומסומנת במפורש כ**טרם מיושמת**. `CLAUDE.md` **לא** עודכן בכוונה — הוא מתאר
מצב קיים, ועדכונו לפני שהקוד נוחת היה גורם לו לתאר התנהגות שאינה קיימת.
**לעדכן אותו יחד עם המימוש.**

#### שיתוף העדות המלאה

`ArchiveVersion` כבר נושא `full_text` ו־`excerpt` — כלומר הטקסט לשיתוף
**קיים**, בניגוד לאינדקס (ראו [R-08](#r-08)).

**✅ הוכרע (27.8):** *"צריך לבדוק את מגבלת התווים בx ושתהיה גרסה לפרסום
במגבלת התווים."* — כלומר לכל עדות תיווצר **גרסת פרסום** שנכנסת למגבלה.

**מגבלות X — לאמת מול התיעוד הרשמי לפני מימוש, הן משתנות:**

| | מגבלה |
| --- | --- |
| חשבון רגיל | **280 תווים** |
| חשבון Premium בתשלום | עד ~25,000 תווים |
| **כל קישור** | נספר כ־**23 תווים** קבועים, בגלל עטיפת `t.co` — גם קישור קצר |

**לתכנן ל־280.** אי אפשר לדעת אם המשתף הוא Premium, וגרסה שנכנסת ל־280
עובדת אצל כולם.

**תקציב התווים בפועל:**

```
280  סה"כ
-23  הקישור לעדות (t.co, קבוע)
- 1  רווח
────
256  נותר לציטוט + ייחוס
```

מתוך 256 יש להפחית את הייחוס (למשל "— Lions of Zion archive" ≈ 24), כך
שנשארים **~230 תווים לציטוט עצמו**.

**דרישות לגרסת הפרסום:**
- לחתוך **בגבול משפט**, לא באמצע מילה או משפט. `substring` עיוור על עדות
  של ניצול הוא בדיוק מה שאסור כאן.
- אם אין משפט שנכנס — לרדת לגבול פסקה או לבחור ציטוט קצר ידנית.
- **שקול שדה `shareQuote` בנתונים.** חיתוך אוטומטי של עדות אישית עלול
  לייצר ציטוט מטעה או קטוע באמצע תיאור קשה. ציטוט שנבחר עריכתית ונשמר
  ברשומה הוא הפתרון הבטוח, והוא גם חד־פעמי.

#### חוזר מ־R-05: מצב הפלטפורמות

X מאפשרת מלל מראש; פייסבוק לא (רק קישור, טקסט מתגי OG); לאינסטגרם אין
web intent כלל. אותן שלוש מסקנות חלות כאן.

#### ⚠️ שאלה שצריכה הכרעה מודעת

הבקשה מוסיפה **הורדה** ו**שיתוף** ל־1,027 פריטי מדיה מארכיוני 7 באוקטובר,
שחלקם קשים. זה מעודד הפצה — וזו הכוונה המוצהרת. שני דברים נגזרים ממנה
שכדאי להכריע עליהם עכשיו ולא אחרי:

1. **קובץ שיורד מנותק מהקשרו.** ברגע שהוא על מחשב, אין לו את הכיתוב, את
   הרשומה או את מקורה. שקול להצמיד את שם הרשומה לשם הקובץ, כך שהוא יישאר
   ניתן לאיתור.
2. **הצירוף עם [R-05](#r-05) — הוסבר לבעלים ב־26.8 לבקשתו.** R-05 מסיר
   מכל רשומה את "Archived from ‹מקור›. Source record: ‹כתובת›". R-10 מוסיף
   הורדה ל־1,027 קבצים. **ביחד** התוצאה היא אלף קבצים שנאספו משני ארכיונים
   חיצוניים, מוצעים להורדה, בלי ששום דבר בעמוד אומר מהיכן הם — וקובץ שיורד
   מגיע בלי מקור ובלי הקשר, כך שמי שיקבל אותו הלאה יניח שהאתר הוא המקור.
   **הבעלים הכריע על שניהם והם עומדים.** נרשם כאן כדי שהתמונה המשותפת תהיה
   גלויה, לא כהסתייגות. ראו סעיף 1 למעלה — שם הקובץ הוא ההפחתה הזולה.
