/** Canonical path for the fact-check desk. Kept in its own module so the page,
 *  the desk, and each row can share it without a cycle through FactCheckDesk. */
export const FACT_CHECK_PATH = "/fact-check";
