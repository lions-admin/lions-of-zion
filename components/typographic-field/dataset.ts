/**
 * TYPOGRAPHIC CORPUS & STREAM ADAPTER
 *
 * Sourced strictly from the canonical typed particle bank (28 categories, 157 handles).
 * Does not read any runtime JSON or external local files.
 */

export * from "./stream-generator";
export * from "@/lib/content/particle-bank";
